array1 = [ 3, 6, 4, 5, 2, 1 ]


// var findMax = function (array1) {
// 	var maxNum;
// 	return maxNum;
// }


let maxNumber = 0;

const numArr = [1,2,3,4,5,6,7,8,20]


	
	for (let i = 0; i < numArr.length; i++) {
		const currentNum = numArr[i];
		
		if (currentNum > maxNumber) maxNumber = currentNum;

		console.log(maxNumber);
		return maxNumber;



	  }